# Build configuration flags. Copy to build.env and set your values.
# Usage: fvm flutter run --dart-define-from-file=build.env
#
# build.env is gitignored — never commit credentials or local overrides.

ENABLE_ON_DEVICE_MODELS=false
ENABLE_GITHUB_ISSUES=false
MAC_APP_STORE_BUILD=false
